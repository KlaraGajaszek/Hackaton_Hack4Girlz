import React from 'react';
import styled from 'styled-components';
import { Post } from './Post';

const Posts = () => {
    return (
        <div>
            <Post />
        </div>
    );
};

export { Posts };
